import 'dart:ui';

class AppColors {

  // ===== DARK =====
  static const base       = Color(0xFF101010);
  static const topBar     = Color(0xFF050505);
  static const layer1     = Color(0xFF0F0F0F);
  static const layer2     = Color(0xFF161616);

  static const border     = Color(0xFF252525);
  static const borderMuted = Color(0xFF1A1A1A);

  static const textMain   = Color(0xFFF0F0F0);
  static const textMuted  = Color(0xFF8A8A8A);
  static const textDim    = Color(0xFF6B6B6B);
  static const textDead   = Color(0xFF3A3A3A);

}